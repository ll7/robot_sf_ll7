# Default DRL-VO baseline config for benchmark evaluation.
# Use this file with `robot_sf_bench run --algo drl_vo --algo-config configs/baselines/drl_vo_default.yaml`.
#
# Artifact status (issue #1960, 2026-06-01): the referenced local checkpoint is
# missing here and has no durable registry/release entry. Treat this config as
# non-runnable and not benchmark evidence until issue #1764 records a durable
# source with checksum or retires the config.

model_id: retired_drl_vo_default_local_only
device: auto
nearest_k: 5
action_space: velocity
v_max: 2.0
omega_max: 1.0
fallback_to_goal: false
goal_speed: 1.0
allow_testing_algorithms: true
