# Canonical high-churn state surface for issue #1475.
#
# Purpose: issue #1475 has had several audit/checker PRs in a short window.
# This append-only state file records the durable closure-audit boundary so
# future agents do not create another blocker-refresh PR or comment stream.
#
# Scope discipline: file tracks durable issue state only. It intentionally does
# not encode transient ready-queue routing, packet lineage routing,
# private ops state, campaign launch state, benchmark results, or paper claims.
issue: 1475
state_surface: orca_residual_bc_smoke_nominal_closure_audit
canonical_evidence:
  closure_audit: docs/context/evidence/issue_1475_closure_audit_2026-07-06.md
  executable_acceptance_audit: docs/context/evidence/issue_1475_acceptance_audit_2026-07-06.json
entries:
  - recorded_at_utc: "2026-07-06T16:38:00Z"
    status: cpu_contract_delivered__external_slurm_rerun_pending
    source:
      issue_url: "https://github.com/ll7/robot_sf_ll7/issues/1475"
      live_issue_state_checked: "open"
      comments_reviewed: 35
      latest_maintainer_comment_utc: "2026-07-05T02:58:46Z"
      latest_maintainer_comment_summary: >-
        PR #4561 merged the CPU-only smoke-to-nominal gate. Remaining action is a
        Slurm-capable owner running one bounded ORCA-residual BC smoke rerun and
        passing that gate before nominal escalation.
    fragmentation_guard: >-
      Pull requests #4608, #4609, and #4661 already merged issue #1475
      closure-audit, follow-up, and executable-audit surfaces in the last 24
      hours. This entry is the single durable state propagation surface for the
      high-churn issue, not another guardrail/checker slice.
    closure_boundary:
      closure_call_for_this_pr: "keep_open"
      reason: >-
        The executable acceptance audit remains fail-closed over tracked evidence:
        smoke evidence is recorded, fallback/degraded success is excluded, but
        durable artifact-pointer status, required smoke telemetry fields, and
        nominal classification still require a Slurm-capable rerun and possible
        nominal escalation. The current run is not authorized to submit compute
        or mutate the issue directly.
      current_authorization_boundary: >-
        No full benchmark campaign run, no Slurm/GPU submission, no paper or
        dissertation claim edit, no issue comment, no merge, no release, and no
        deletion.
    acceptance_evidence:
      - criterion: "Residual dataset manifest NPZ recorded durable artifacts."
        status: partially_met
        evidence: >-
          The job-12913 source checksum manifest records the smoke NPZ checksum,
          but the tracked smoke summary has no durable artifact_pointer_status,
          so durable pointer completion is not proven.
      - criterion: "Learned residual checkpoint pointer durable included in completion update."
        status: partially_met
        evidence: >-
          The job-12913 source checksum manifest records the smoke checkpoint
          checksum, but the tracked smoke summary has no durable
          artifact_pointer_status, so checkpoint pointer completion is not
          proven.
      - criterion: "Diagnostics report includes ORCA command, raw residual, bounded residual, final guarded command, residual clipping rate, guard veto rate, fallback/degraded status."
        status: not_met
        evidence: >-
          The executable audit reports missing residual_clipping_rate,
          guard_veto_rate, fallback_degraded_status, and artifact_pointer_status
          in the tracked smoke summary.
      - criterion: "Fallback/degraded rows are not counted learned-residual success evidence."
        status: met
        evidence: >-
          PR #4561 added validate_smoke_nominal_gate(), which fails closed when
          fallback/degraded status is not clear or artifact pointers are not
          durable/complete.
      - criterion: "Smoke result recorded before nominal escalation."
        status: met
        evidence: >-
          The job-12913 smoke result is durably tracked as failed_closed with
          nominal_escalation_allowed=false.
      - criterion: "Nominal result classified ready #1358 continuation, revise, stop."
        status: not_met
        evidence: >-
          No nominal result exists in tracked evidence. The smoke gate remains
          invalid over the tracked job-12913 summary.
    next_empirical_action: >-
      On a Slurm-capable lane, run one bounded ORCA-residual BC smoke rerun
      against the current post-gate code. Only if validate_smoke_nominal_gate()
      passes, escalate the bounded nominal job and record the #1358
      continuation/revise/stop classification.
    validation:
      - command: "gh api repos/ll7/robot_sf_ll7/issues/1475 and comments"
        result: "Issue body and all 35 comments reviewed through REST API."
      - command: "jq docs/context/evidence/issue_1475_acceptance_audit_2026-07-06.json"
        result: "Executable audit reports status=blocked, closure_call=keep_open."
    out_of_scope_confirmed:
      full_benchmark_campaign_run: false
      slurm_or_gpu_submission: false
      paper_or_dissertation_claim_edit: false
      issue_comment: false
      release: false
      merge: false
      deletion: false
  - recorded_at_utc: "2026-07-07T02:50:00Z"
    status: merged_pr_evidence_audit_delivered__external_slurm_rerun_pending
    source:
      issue_url: "https://github.com/ll7/robot_sf_ll7/issues/1475"
      live_issue_state_checked: "open"
      latest_maintainer_comment_utc: "2026-07-05T02:58:46Z"
      latest_maintainer_comment_summary: >-
        PR #4561 merged CPU-only smoke-to-nominal gate. Remaining action
        remains one bounded ORCA-residual behavior-cloning smoke rerun on a
        Slurm-capable host before any nominal escalation.
      merged_prs_reviewed:
        - "#4561"
        - "#4661"
        - "#4667"
        - "#4678"
        - "#4721"
    fragmentation_guard: >-
      Multiple issue #1475 guardrail/checker/state PRs merged in the last 24
      hours. This entry records the new executable merged-PR evidence table in
      the canonical state surface, not a second comment stream or transient
      queue-routing state.
    closure_boundary:
      closure_call_for_this_pr: "keep_open"
      reason: >-
        The executable audit now maps merged PRs to acceptance evidence, but
        tracked smoke evidence still fails the nominal gate and compute
        submission is out of scope for this PR.
    current_authorization_boundary: >-
      No full benchmark campaign run, no Slurm/GPU submission, no paper or
      dissertation claim edit, no issue comment, no merge, no release, no
      deletion.
    acceptance_evidence:
      - criterion: "Residual dataset manifest NPZ recorded durable artifacts."
        status: partially_met
        evidence: >-
          job-12913 source checksum manifest records smoke NPZ checksum, but
          tracked smoke summary no durable artifact_pointer_status, so durable
          pointer completion is not proven.
      - criterion: "Learned residual checkpoint pointer durable included in completion update."
        status: partially_met
        evidence: >-
          job-12913 source checksum manifest records smoke checkpoint checksum,
          but tracked smoke summary no durable artifact_pointer_status, so
          durable checkpoint pointer completion is not proven.
      - criterion: "Diagnostics report includes ORCA command, raw residual, bounded residual, final guarded command, residual clipping rate, guard veto rate, fallback/degraded status."
        status: not_met
        evidence: >-
          Executable audit reports missing residual_clipping_rate,
          guard_veto_rate, fallback_degraded_status, artifact_pointer_status in
          tracked smoke summary.
      - criterion: "Fallback/degraded rows are not counted learned-residual success evidence."
        status: met
        evidence: >-
          validate_smoke_nominal_gate() remains invalid over incomplete tracked
          smoke evidence, so fallback/degraded rows are excluded from positive
          learned-residual success evidence.
      - criterion: "Smoke result recorded before nominal escalation."
        status: met
        evidence: >-
          job-12913 smoke result is durably tracked as failed_closed with
          nominal_escalation_allowed=false.
      - criterion: "Nominal result classified ready #1358 continuation, revise, stop."
        status: not_met
        evidence: >-
          No nominal result exists in tracked evidence; smoke gate remains
          invalid over tracked job-12913 summary.
    merged_pr_evidence:
      - pr: "#4561"
        closure_effect: partial
        evidence: "CPU-only smoke nominal gate delivered."
      - pr: "#4661"
        closure_effect: partial
        evidence: "Executable acceptance audit delivered."
      - pr: "#4667"
        closure_effect: partial
        evidence: "Canonical high-churn state surface delivered."
      - pr: "#4678"
        closure_effect: partial
        evidence: "State surface verification delivered."
      - pr: "#4721"
        closure_effect: partial_keep_open
        evidence: "Integration report delivered; issue remains externally smoke-blocked."
    integration_report:
      status: blocked
      blockers_remaining:
        - "Durable artifact_pointer_status not proven for dataset/checkpoint outputs."
        - "Tracked smoke summary lacks required residual/guard/fallback fields."
        - "No nominal result classification exists."
      blockers_new: []
      blockers_intentional:
        - "No Slurm/GPU submission in PR."
        - "No nominal escalation while smoke gate remains invalid."
      next_empirical_action: >-
        On a Slurm-capable lane, run one bounded ORCA-residual behavior-cloning
        smoke rerun against current post-gate code. Only if
        validate_smoke_nominal_gate() passes, escalate bounded nominal and
        record #1358 continuation/revise/stop classification.
    validation:
      - command: "uv run python scripts/validation/build_issue_1475_acceptance_audit.py --write"
        result: "Regenerates JSON with merged_pr_evidence and status=blocked."
    out_of_scope_confirmed:
      full_benchmark_campaign_run: false
      slurm_or_gpu_submission: false
      paper_or_dissertation_claim_edit: false
      issue_comment: false
      release: false
      merge: false
      deletion: false
  - recorded_at_utc: "2026-07-07T02:55:00Z"
    status: merged_pr_evidence_audit_delivered__external_slurm_rerun_pending
    source:
      issue_url: "https://github.com/ll7/robot_sf_ll7/issues/1475"
      live_issue_state_checked: "open"
      comments_reviewed: 35
      latest_maintainer_comment_utc: "2026-07-05T02:58:46Z"
      latest_maintainer_comment_summary: >-
        PR #4561 merged CPU-only smoke-to-nominal gate. Remaining action remains one
        bounded ORCA-residual BC smoke rerun on a Slurm-capable host before any
        nominal escalation.
      fragmentation_guard: >-
        Multiple issue #1475 audit/state PRs already merged in the last 24 hours;
        this entry records the consolidated integration-report slice, not another
        readiness packet or transient queue route.
    closure_boundary:
      closure_call_for_this_pr: "keep_open"
      reason: >-
        Executable acceptance audit still reports blocked over tracked evidence.
        Current authorization forbids compute submission, and local.machine.md
        records allow_slurm_submission: false, so the remaining empirical smoke
        rerun cannot be performed in this PR.
      current_authorization_boundary: >-
        No full benchmark campaign run, no Slurm/GPU submission, no paper or
        dissertation claim edit, no issue comment, no merge, no release, no
        deletion.
    acceptance_evidence:
      - criterion: "Residual dataset manifest NPZ recorded durable artifacts."
        status: partially_met
        evidence: >-
          job-12913 source checksum manifest records smoke NPZ checksum, but
          tracked smoke summary no durable artifact_pointer_status, so durable
          pointer completion is not proven.
      - criterion: "Learned residual checkpoint pointer durable included in completion update."
        status: partially_met
        evidence: >-
          job-12913 source checksum manifest records smoke checkpoint checksum, but
          tracked smoke summary no durable artifact_pointer_status, so durable
          checkpoint pointer completion is not proven.
      - criterion: "Diagnostics report includes ORCA command, raw residual, bounded residual, final guarded command, residual clipping rate, guard veto rate, fallback/degraded status."
        status: not_met
        evidence: >-
          Executable audit reports missing residual_clipping_rate, guard_veto_rate,
          fallback_degraded_status, and artifact_pointer_status in tracked smoke
          summary.
      - criterion: "Fallback/degraded rows are not counted learned-residual success evidence."
        status: met
        evidence: >-
          validate_smoke_nominal_gate() remains invalid over incomplete tracked
          smoke evidence, so fallback/degraded rows are excluded from positive
          learned-residual success evidence.
      - criterion: "Smoke result recorded before nominal escalation."
        status: met
        evidence: >-
          job-12913 smoke result is durably tracked as failed_closed with
          nominal_escalation_allowed=false.
      - criterion: "Nominal result classified ready #1358 continuation, revise, stop."
        status: not_met
        evidence: >-
          No nominal result exists in tracked evidence; smoke gate remains invalid
          over tracked job-12913 summary.
    integration_report:
      status: blocked
      blockers_remaining:
        - "Durable artifact_pointer_status not proven for dataset/checkpoint outputs."
        - "Tracked smoke summary lacks required residual/guard/fallback fields."
        - "No nominal result classification exists."
      blockers_new: []
      blockers_intentional:
        - "No Slurm/GPU submission in this PR."
        - "No nominal escalation while smoke gate remains invalid."
    next_empirical_action: >-
      On a Slurm-capable lane, run one bounded ORCA-residual BC smoke rerun against
      current post-gate code. Only if validate_smoke_nominal_gate() passes, escalate
      bounded nominal and record #1358 continuation/revise/stop classification.
    validation:
      - command: "uv run python scripts/validation/build_issue_1475_acceptance_audit.py --write"
        result: "Regenerates JSON with integration_report.status=blocked."
    out_of_scope_confirmed:
      full_benchmark_campaign_run: false
      slurm_or_gpu_submission: false
      paper_or_dissertation_claim_edit: false
      issue_comment: false
      release: false
      merge: false
      deletion: false
