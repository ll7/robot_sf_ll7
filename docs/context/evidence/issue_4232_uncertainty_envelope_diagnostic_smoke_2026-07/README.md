# Issue #4232 Uncertainty-Envelope Diagnostic Smoke Evidence

This directory records the closure-audit diagnostic slice for issue #4232. The run executed one
CPU-local `prediction_mpc` scenario/seed slice across the pre-registered envelope-off alpha-zero,
envelope-on alpha-zero, and envelope-on alpha-0.10 arms.

Raw episode JavaScript Object Notation Lines (JSONL), videos, Slurm logs, checkpoints, and model
caches are intentionally excluded. The compact row input is tracked as
`compact_alpha_sweep_rows.json`.

## Closure Audit

| Acceptance criterion | Evidence | Status |
| --- | --- | --- |
| Define benchmark or campaign scope before making uncertainty-envelope performance or safety claims. | PR #4256 added `configs/benchmarks/issue_4232_uncertainty_envelope_claim_packet.yaml` and `scripts/validation/check_issue_4232_uncertainty_envelope_claim_packet.py`; `pre_registration_packet.json` preserves the validated packet used for this run. | Met for diagnostic/no-claim boundary. |
| Record seeds, scenario set, planner settings, speed range/runtime constraints, and artifact provenance. | PR #4256 records scenario surface, seed policy, planner family, alpha arms, horizon, time step, and row-status policy; this bundle adds `metadata.json`, `compact_alpha_sweep_rows.json`, `runtime_cost_report.csv`, and `SHA256SUMS`. | Met for the one-scenario CPU diagnostic slice. |
| Update relevant claim map, benchmark report, or paper-facing text only if evidence supports the claim boundary. | `claim_readiness.md` keeps the result diagnostic-only and not ready for benchmark-strength, conformal, real-world safety, deployment, paper, or dissertation claims. No claim map, benchmark report, or paper-facing text is promoted by this slice. | Met as a no-promotion gate. |
| Keep fallback/degraded execution excluded from benchmark-strength evidence. | PR #4281 added compact evidence tables; `row_status_audit.csv` shows all three rows are `diagnostic_only`, with zero benchmark-strength rows. | Met. |
| Run an actual diagnostic slice before any claim-map/report/paper/dissertation update. | PR #4521 added the CPU-local runner. This bundle was generated by `scripts/benchmark/run_issue_4232_uncertainty_envelope_alpha_sweep.py --max-scenarios 1 --max-seeds 1 --json` after merging current `origin/main`. | Met for diagnostic smoke only. |

## Claim Boundary

This evidence does not establish a full benchmark campaign result, conformal calibration,
real-world safety, deployment readiness, generalized planner superiority, paper claims, or
dissertation claims. The nonzero-alpha row now has measured activation telemetry with
`no_mechanism_activation`, so alpha-effect mechanism interpretation remains blocked until a
follow-up diagnostic slice observes actual envelope activation on a suitable scenario/planner row.
