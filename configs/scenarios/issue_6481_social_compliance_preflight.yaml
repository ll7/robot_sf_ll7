# Issue #6481 social-compliance preflight scenario matrix.
#
# Selects exactly one small scenario with pedestrians so the diagnostic
# social-compliance emission from PR #6236 can be exercised natively.
# This is a preflight-only surface; it does not establish planner ranking,
# fairness, or social-validity claims.

schema_version: robot_sf.scenario_matrix.v1
includes:
  - sets/verified_simple_subset_v1.yaml
select_scenarios:
  - single_ped_crossing_orthogonal
map_search_paths:
  - ../../maps/svg_maps
