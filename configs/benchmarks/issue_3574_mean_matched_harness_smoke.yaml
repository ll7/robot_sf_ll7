schema_version: mean_matched_heterogeneity_harness.config.v1
issue: 3574
claim_boundary: harness_only_no_ablation_result
trace_metric_keys:
  - clearance_m
  - near_field_exposure_s
planners:
  - key: goal
    algo: goal
  - key: social_force
    algo: social_force
  - key: orca
    algo: orca
seeds:
  - 101
  - 102
  - 103
# Fractions of pedestrians that receive no robot-avoidance force.  The manifest
# expands every mean-matched arm across this fixed-density sweep; executing it
# remains a separate, campaign-only step.
response_law_fractions:
  - 0.0
  - 0.1
  - 0.25
  - 0.5
scenarios:
  - id: issue_3574_classic_crossing_density_002
    density: 0.02
    pedestrian_control_trace_near_field_clearance_m: 1.0
    population_size: 12
    archetype_seed: 3574
    response_law_seed: 3574
    composition:
      cautious: 0.25
      standard: 0.5
      hurried: 0.25
    archetypes:
      cautious:
        desired_speed_factor: 0.7
        radius_m: 0.35
      standard:
        desired_speed_factor: 1.0
        radius_m: 0.3
      hurried:
        desired_speed_factor: 1.4
        radius_m: 0.25
