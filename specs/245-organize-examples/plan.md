# Implementation Plan: Organize and Categorize Example Files

**Branch**: `245-organize-examples` | **Date**: November 12, 2025 | **Spec**: [`specs/245-organize-examples/spec.md`](./spec.md)
**Input**: Feature specification from `/specs/245-organize-examples/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.

## Summary

Reorganize the 84 example scripts into a tiered structure (`quickstart/`, `advanced/`, `benchmarks/`, `plotting/`, `_archived/`), enrich each active script with docstrings, add an `examples/README.md` with a decision tree, and introduce automated validation so every highlighted example runs without errors.

## Technical Context

**Language/Version**: Python 3.11  
**Primary Dependencies**: Python standard library (`pathlib`, `shutil`, `textwrap`), existing `robot_sf` modules, documentation tooling  
**Storage**: Git-tracked file system (source tree)  
**Testing**: pytest-based suite with parametrized example smoke module (`tests/examples/test_examples_run.py`)  
**Target Platform**: Cross-platform developer environments (macOS/Linux CLI)  
**Project Type**: Python research toolkit / library  
**Performance Goals**: Ensure full example harness completes under 10 minutes in CI by default; per-example metadata can opt out rare interactive cases with documented rationale  
**Constraints**: Preserve reproducibility contracts, maintain stable import paths, align with documentation governance  
**Scale/Scope**: 84 existing example scripts plus associated docs/links

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

- **Principle I (Reproducible Social Navigation Core)**: Directory reshuffle must remain scriptable and ensure every example can still be reproduced from repo state.
- **Principle VIII (Documentation as an API Surface)**: Creating `examples/README.md` and updating references is mandatory; failure would violate this gate.
- **Principle XI (Library Reuse & Helper Documentation)**: Examples must continue to rely on existing `robot_sf` helpers without introducing bespoke logic.
- **Principle XII (Preferred Logging & Observability)**: Any harness or script added must use Loguru inside library code if logging is needed.

**Gate Status**: PASS — No planned activity conflicts with constitutional principles; documentation and reproducibility obligations are explicitly tracked in requirements.

**Post-Design Re-evaluation**: After drafting research, data model, contracts, and quickstart guidance, the updated plan continues to satisfy Principles I, VIII, XI, and XII. The manifest-driven automation reinforces reproducibility, documentation deliverables are enumerated, and no library-level logging changes are introduced outside the Loguru convention. Gate remains PASS.

## Project Structure

### Documentation (this feature)

```text
specs/245-organize-examples/
├── spec.md
├── plan.md                  # /speckit.plan output (this file)
├── research.md              # Phase 0 research summary
├── data-model.md            # Phase 1 entity definitions
├── quickstart.md            # Phase 1 implementation checklist
├── contracts/
│   └── examples-openapi.yaml
├── checklists/
│   └── requirements.md
└── tasks.md                 # To be generated by /speckit.tasks (Phase 2)
```

### Source Code (repository root)
<!--
  ACTION REQUIRED: Replace the placeholder tree below with the concrete layout
  for this feature. Delete unused options and expand the chosen structure with
  real paths (e.g., apps/admin, packages/something). The delivered plan must
  not include Option labels.
-->

```text
examples/
├── README.md                   # decision tree + index generated from manifest
├── examples_manifest.yaml      # source of truth for categories and CI flags
├── quickstart/
├── advanced/
├── benchmarks/
├── plotting/
└── _archived/

docs/
├── README.md                   # link to examples guide
└── dev_guide.md                # reference restructured examples section

tests/examples/
└── test_examples_run.py        # pytest parametrized smoke harness

scripts/validation/
└── run_examples_smoke.py       # optional CLI wrapper that reuses pytest entry point
```

**Structure Decision**: Adopt the tiered directory layout and manifest-driven metadata under `examples/`, update documentation entry points, and add reusable automation (pytest module plus optional CLI wrapper) to execute CI-approved examples.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| _None_ | _N/A_ | No constitutional violations identified |
