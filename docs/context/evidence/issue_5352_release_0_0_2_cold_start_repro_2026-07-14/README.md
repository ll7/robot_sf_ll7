<!-- AI-GENERATED (robot_sf#5352, 2026-07-14) - NEEDS-REVIEW -->

# Issue #5352 — Independent cold-start reproduction evidence (release 0.0.2)

This directory holds **real, executed** evidence for issue #5352, produced by a
cheap-lane worker on 2026-07-14. It is the artifact-first companion to the
prior `--local-repo` (partial) report promoted in #5366.

## What was actually executed

1. **Clean release-tag clone** — `git clone --branch 0.0.2 --depth 1` of
   `ll7/robot_sf_ll7`, without `--local-repo`. The clone step resolved to tag
   commit `cbeaca6109654b4053c19542a0a17ed656a387a6` (the `0.0.2` tag).
2. **Release-bundle checksum verification** — downloaded the published bundle
   `paper_experiment_matrix_7planners_v1_release_v0_0_2_..._publication_bundle.tar.gz`
   from the GitHub release `0.0.2` and verified it against
   `configs/releases/release_0_0_2_checksum_manifest.yaml`.
   - Bundle SHA-256: `64e8510ab7ba934103c709907f66a783c7b3dd2dd58aa4bd725e762da2734d90`
     — **exact match**.
   - All 4 embedded artifacts (`publication_manifest`, `checksums`,
     `snqi_diagnostics_json`, `snqi_diagnostics_md`) — **exact match**.
3. **Cold-start reproduction report** — generated by
   `scripts/repro/cold_start_reproduction_report.py --tag 0.0.2 --checksums-only`
   from the real clone, yielding `overall_verdict: pass` (clone + verify pass).

## Verdict

- `checksum_verification_report.json`: `overall_verdict = pass`
- `cold_start_reproduction_report.json`: `overall_verdict = pass`

## Genuine instruction gaps found (deliverables, not failures)

A clean machine following the issue's public instructions literally cannot
perform step 2–3 at the **`0.0.2` tag**, because the verification harness and
manifest do not exist at that tag:

- `configs/releases/release_0_0_2_checksum_manifest.yaml` — **absent at `0.0.2`**,
  first present at tag `0.0.3`.
- `scripts/repro/verify_release_checksums.py` and
  `scripts/repro/cold_start_reproduction_report.py` — **absent at `0.0.2`**,
  first present at `0.0.3`.
- `configs/benchmarks/releases/*0_0_2_scoped.yaml` (the subset config referenced
  by the report) — **absent at `0.0.2`**.

So the release *design* is replayable, but only from `main`/`0.0.3`, not from a
zero-context clone at the release tag. A clean machine must (a) build the env
from `uv.lock` at `0.0.2` (that file *is* present at the tag), then (b) obtain
the reproduction tooling + manifest from `0.0.3` or `main`. This is the concrete
instruction gap that should be filed as an issue and, ideally, back-patched into
the `0.0.2` tag or documented in `docs/benchmark_release_0_0_2_reproduction.md`.

## What remains unresolved (not automatable on the dev host)

- The issue's full acceptance asks for a **non-development machine/person** to
  follow only public instructions end-to-end (incl. the pre-registered numeric
  subset run, not just checksums). That external execution was out of scope for
  this cheap-lane run; the evidence here is generated on the development host and
  is therefore a `pass` on *checksum verification*, not the fully independent
  third-party run. The claim should be scoped as: "release bundle checksums and
  embedded artifacts independently verified against the authoritative manifest
  via a clean release-tag clone," not "fully independent third-party replay."
- The numeric subset reproduction (criterion: exact-hash vs near-exact tolerance
  per the manifest `reproducibility_contract`) still requires a clean-machine run
  and was intentionally limited to `--checksums-only` here to stay CPU-cheap.

<!-- /AI-GENERATED -->
