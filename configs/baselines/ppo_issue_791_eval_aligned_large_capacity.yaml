# PPO issue-791 Wave-5 leader config pinned to the eval-aligned large-capacity candidate.
#
# Intended for camera-ready benchmark comparisons where PPO should use the
# issue-791 leader through the model registry so missing local artifacts can be
# downloaded from W&B deterministically.

model_id: ppo_expert_issue_791_reward_curriculum_eval_aligned_large_capacity_20260417

# Device selection for SB3
device: auto
# Use deterministic=True for evaluation/benchmarking
deterministic: true

# Observation handling
obs_mode: dict

# Action formatting
action_space: unicycle
v_max: 2.0
omega_max: 1.0

# Fail fast so camera-ready comparisons stay benchmark-success only.
fallback_to_goal: false
