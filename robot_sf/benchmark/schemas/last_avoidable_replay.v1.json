{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "last_avoidable_replay.v1.json",
  "title": "Last Avoidable Replay v1",
  "description": "Source-tagged offline result of frozen-state counterfactual replay (issue #5442). Controlled fixtures and native simulator adapters may emit this same contract; config.source_kind and claim_boundary keep their source and claim boundaries explicit. The report records whether a collision was avoidable, the earliest avoidable unsafe control action (t_uca) and the point of no return (t_inevitable), the versioned analysis config, the baseline determinism check, and every per-decision-point branch result. This is diagnostic replay evidence only, not a real-episode root-cause, benchmark, or paper-grade claim, and it assigns no legal or moral fault. config.t_contact is the declared contact state tick; determinism.observed_contact_steps are the replay observations that a downstream join must verify before exposing a contact timestamp.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "verdict",
    "normative_fault",
    "claim_boundary",
    "t_danger",
    "t_uca",
    "t_inevitable",
    "t_contact",
    "feasible_coverage",
    "abstained",
    "abstain_reason",
    "config",
    "determinism",
    "branches",
    "minimal_sufficient_interventions",
    "runtime_s",
    "notes"
  ],
  "properties": {
    "schema_version": {
      "const": "last_avoidable_replay.v1"
    },
    "verdict": {
      "type": "string",
      "enum": ["avoidable", "already_unavoidable", "unknown"]
    },
    "normative_fault": {
      "const": "not_assessed"
    },
    "claim_boundary": {
      "description": "Source-sensitive claim boundary for this diagnostic replay; schema validity does not promote the result to a causal, benchmark, or paper-facing claim.",
      "type": "string",
      "minLength": 1
    },
    "t_danger": {
      "type": "integer",
      "minimum": 0
    },
    "t_uca": {
      "type": ["integer", "null"],
      "minimum": 0
    },
    "t_inevitable": {
      "type": ["integer", "null"],
      "minimum": 0
    },
    "t_contact": {
      "description": "Declared baseline contact state tick; downstream joins expose it as observed only when determinism.observed_contact_steps agree exactly.",
      "type": "integer",
      "minimum": 1
    },
    "feasible_coverage": {
      "type": "number",
      "minimum": 0.0,
      "maximum": 1.0
    },
    "abstained": {
      "type": "boolean"
    },
    "abstain_reason": {
      "type": ["string", "null"]
    },
    "config": {
      "type": "object",
      "additionalProperties": false,
      "description": "The source_kind property is optional for backward compatibility with earlier last_avoidable_replay.v1 payloads; new producers should emit an explicit source classification.",
      "required": [
        "t_danger",
        "t_contact",
        "horizon",
        "substitution_mode",
        "determinism_replays",
        "action_set_id",
        "feasibility_filter",
        "collision_predicate",
        "pedestrian_response"
      ],
      "properties": {
        "t_danger": {"type": "integer", "minimum": 0},
        "t_contact": {
          "description": "Declared baseline contact state tick used to bound replay.",
          "type": "integer",
          "minimum": 1
        },
        "horizon": {"type": "integer", "minimum": 1},
        "substitution_mode": {"type": "string", "enum": ["single_step", "hold"]},
        "determinism_replays": {"type": "integer", "minimum": 1},
        "source_kind": {
          "description": "Explicit source classification for new payloads; omitted in legacy v1 payloads.",
          "type": "string",
          "minLength": 1
        },
        "action_set_id": {"type": "string", "minLength": 1},
        "feasibility_filter": {"type": "string", "minLength": 1},
        "collision_predicate": {"type": "string", "minLength": 1},
        "pedestrian_response": {"type": "string", "minLength": 1}
      }
    },
    "determinism": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "replays",
        "collision_stable",
        "contact_step_stable",
        "deterministic",
        "observed_contact_steps"
      ],
      "properties": {
        "replays": {"type": "integer", "minimum": 1},
        "collision_stable": {"type": "boolean"},
        "contact_step_stable": {"type": "boolean"},
        "deterministic": {"type": "boolean"},
        "observed_contact_steps": {
          "description": "Contact state ticks observed across baseline replays; a single declared tick is verified only when all entries agree.",
          "type": "array",
          "items": {"type": ["integer", "null"], "minimum": 0}
        }
      }
    },
    "branches": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["step", "feasible_count", "any_prevented", "preventing_action_labels"],
        "properties": {
          "step": {"type": "integer", "minimum": 0},
          "feasible_count": {"type": "integer", "minimum": 0},
          "any_prevented": {"type": "boolean"},
          "preventing_action_labels": {
            "type": "array",
            "items": {"type": "string"}
          }
        }
      }
    },
    "minimal_sufficient_interventions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["step", "action_label", "substitution_mode", "horizon"],
        "properties": {
          "step": {"type": "integer", "minimum": 0},
          "action_label": {"type": "string", "minLength": 1},
          "substitution_mode": {"type": "string", "enum": ["single_step", "hold"]},
          "horizon": {"type": "integer", "minimum": 1}
        }
      }
    },
    "runtime_s": {
      "type": ["number", "null"],
      "minimum": 0.0
    },
    "notes": {
      "type": "array",
      "items": {"type": "string"}
    }
  }
}
