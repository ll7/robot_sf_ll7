<!-- AI-GENERATED NEEDS-REVIEW: independent re-validation evidence; reviewer inspection still required. -->
# Issue #5443 — Covered-By-PR Re-Validation (2026-07-24)

Issue: <https://github.com/ll7/robot_sf_ll7/issues/5443>
Covering PR: <https://github.com/ll7/robot_sf_ll7/pull/6173> (merged to `origin/main`)

## Plain-language summary

This is a re-validation record, not new code. It independently re-checks that the
merged implementation of issue #5443 — a deterministic, label-blind analyser that
recovers the cause of an injected collision from low-level trace signals — still
passes every code-checkable acceptance criterion from a clean `origin/main` head.
Conclusion: it does. The issue is **covered by PR #6173**; re-implementing it would
duplicate merged, passing code.

## Why a re-validation record instead of new code

A 2026-07-22 decision brief on issue #5443 asked whether the 14 frozen fault-injection
scenarios (and the rule-based mapping across the 10 cause classes) should be implemented
as kinematic scenarios in `last_avoidable_fixtures.py` with a rule-based translation
from replay branches, or as full simulator configs. PR #6173 chose the former and merged
it to `origin/main`:

- `robot_sf/benchmark/collision_cause_analyser.py` — the rule-based cause analyser.
- `robot_sf/benchmark/last_avoidable_fixtures.py` — the 14 `KinematicScenario` fixtures
  plus `ObservableTraceEvent` records and counterfactual repairs.
- `robot_sf/benchmark/schemas/collision_cause_analyser_run.v1.json` — the run payload schema.
- `scripts/analysis/run_collision_cause_attribution_issue_5443.py` — the analyser + scorer CLI.
- `tests/benchmark/test_collision_cause_analyser_issue_5443.py` — the analyser test suite.

Per the repository's issue-implementation guidance, when an open or merged PR already
covers an issue's stated boundary and its proof passes on the current head, the issue is
classified `covered_by_pr` and is **not** re-implemented. This note records that
determination and the fresh proof that supports it.

The analyser consumes the existing last-avoidable counterfactual replay
(`last_avoidable_replay.py`, issue #5442) and emits `AttributionVerdict` objects against
the 10-class cause schema owned by the fail-closed scoring harness
(`collision_cause_attribution.py`, issue #5455). Neither of those files is touched here.

## Claim boundary and evidence status

- Claim boundary: controlled-fixture injected-fault validation only. This is **not** a
  held-out real-trace study, campaign result, production root-cause claim, or assignment
  of legal or moral fault.
- Evidence status: `nominal` for the controlled-fixture contract. The analyser is
  deterministic and reproducible from the head below.
- Major caveats: criterion 6 (two independent human reviewers inspecting a frozen sample)
  remains an open process step and is outside any code PR. A later held-out real-trace
  study is required before any paper-facing attribution claim.
- Confidence: high for the controlled-fixture re-validation (deterministic, reproducible,
  all code-checkable criteria pass).

## Independent re-validation (clean `origin/main` head)

Head: `959e55d9b10895d961f9ff10cd4cab24f8b32a8a` (`git_tree_dirty: false`).
Manifest SHA-256: `af411147f577ddd3da84d070ec5e3eab52a51100cef6248cbcfb504947c50dfc`
Run-payload schema SHA-256: `061e81fc7f447e9f6ac95a0255d9d13ff51e00db3ead4ddef9623fb738a30c55`
Analyser-config SHA-256: `defe41b1dff8bb3697192038443138f4713d5c2162d05e5ae3e905640f6612ee`

Commands run from the clean head:

```bash
uv run pytest -q tests/benchmark -k 'causal and attribution'          # 51 passed
uv run python scripts/analysis/run_collision_cause_attribution_issue_5443.py \
    --verdicts <out>/verdicts.json --score --out <out>/report.json      # exit 0
uv run python scripts/analysis/validate_collision_cause_attribution_issue_5443.py \
    --verdicts <out>/verdicts.json                                      # exit 0
uv run ruff check robot_sf/benchmark scripts/analysis tests/benchmark  # clean
git diff --check                                                        # clean
```

Scored report (`status: scored`, `verdict: pass`, `n_fixtures: 14`):

- `top_explanation_accuracy: 1.0` (criterion 3 requires >= 0.90).
- `median_temporal_localization_error: 0.0` steps (criterion 3 allows <= 1 step).
- `avoidability_accuracy: 1.0`; `abstention_coverage: 1.0`.
- `ambiguous_high_confidence_violations: []` (criterion 4 — ambiguous fixtures never get
  a high-confidence single-cause verdict).
- `negative_control_promotions: []` (criterion 5 — negative controls are not promoted).
- Per-class precision and recall are 1.0 across all 10 cause classes.

The schema-validated run payload is regenerated by the CLI above (it carries the exact
commit, manifest, analyser-config, and schema hashes), so it is intentionally not copied
into this note.

## Acceptance-criteria status

- [x] 1. Frozen manifest of cause class, activation window, intervention, ambiguity.
- [x] 2. Report precision/recall, top-explanation accuracy, temporal localization,
      avoidability, determinism, abstention, and calibration.
- [x] 3. Simple injected causes reach >= 0.90 top-explanation accuracy and <= 1-step
      median temporal error.
- [x] 4. Ambiguous fixtures never receive a high-confidence single-cause verdict.
- [x] 5. Negative controls are not promoted to an actual cause.
- [ ] 6. Two reviewers independently inspect a frozen sample (open process step).
- [x] 7. Compact evidence is schema-validated and bound to commit/config hashes.

## Remaining work

- Criterion 6: independent two-reviewer inspection of the frozen sample (human process).
- A held-out real-trace study before any paper-facing or production attribution claim.
- A known fixture-realism limitation: `already_unavoidable_contact` localizes to the
  replay `t_inevitable` rather than the manifest's nominal activation window; it does not
  change the current median and is tracked separately.
