# Experimental SAC baseline config for benchmark smoke/evaluation runs.
#
# This points at the benchmark-compatible `socnav_struct` training profile.
# Do not promote to paper-facing matrices until checkpoint-specific quality-gate
# evidence has been recorded.

model_path: output/models/sac/sac_gate_socnav_struct_v1.zip

device: auto
deterministic: true

obs_mode: dict
action_space: unicycle
v_max: 2.0
omega_max: 1.0

fallback_to_goal: false
profile: experimental
