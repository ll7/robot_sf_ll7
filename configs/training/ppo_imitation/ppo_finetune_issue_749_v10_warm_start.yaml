# Issue #749 PPO fine-tune config for the BC-preinitialized challenger.
#
# Shared settings live in ``ppo_finetune_issue_749_warm_start_base.yaml`` so
# the clean rerun can preserve the same effective launch contract.
base_config: ppo_finetune_issue_749_warm_start_base.yaml
run_id: issue_749_ppo_finetune_v10_warm_start
