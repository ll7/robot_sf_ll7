# Issue-852 single-factor ablation: attention ONLY (10M fixed-seed promotion).
#
# Generated as a sibling of expert_ppo_issue_791_baseline_promotion_10m_env22_eval_aligned.yaml.
# This config changes exactly one feature family and keeps the eval-aligned
# no-feature baseline recipe otherwise intact. Historical issue-791 promotion
# configs remain unmodified run-history artifacts.
#
# Submit with:
#   ISSUE791_TRAIN_CONFIG=configs/training/ppo/ablations/single_factor/attention_only_10m_env22_seed231.yaml scripts/dev/sbatch_use_max_time.sh SLURM/Auxme/issue_791_attention_head.sl
base_config: attention_only_base.yaml
policy_id: ppo_expert_issue_852_attention_only_10m_env22_seed231
seeds:
- 231
tracking:
  tensorboard: false
  wandb:
    enabled: true
    project: robot_sf
    group: issue-852-single-factor-attention
    job_type: expert-ppo-10m-single-factor-attention-seed231
    tags:
    - issue-852
    - issue-791-followup
    - single-factor-attention
    - ppo
    - promotion-10m-env22
    - eval-aligned
    - seed-231
    - fixed-seed
