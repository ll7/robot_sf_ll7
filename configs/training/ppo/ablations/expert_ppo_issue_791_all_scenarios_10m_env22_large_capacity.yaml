# Issue-791 "all available scenarios" training run using the Wave-5 leader recipe.
# Scenario set: configs/scenarios/sets/ppo_all_available_training_v1.yaml
# (maintained superset + verified_simple + safety_barrier_static + TEB topology).
# Recipe: curriculum + large capacity + predictive foresight + randomized seeds,
# same PPO hyperparams as the 11724 leader (success=0.929 on the narrow eval).
# Purpose: broader scenario exposure for the best-known recipe; paired with the
# Wave-5 leader as a robustness probe for the camera-ready benchmark matrix.
# Inherits the shared issue-791 all-scenarios recipe; keeps the explicit
# env_factory_kwargs.peds_have_obstacle_forces override the seed replicas omit.
base_config: expert_ppo_issue_791_all_scenarios_10m_env22_large_capacity_base.yaml
policy_id: ppo_expert_issue_791_all_scenarios_10m_env22_large_capacity
seeds:
- 123
env_factory_kwargs:
  peds_have_obstacle_forces: true
tracking:
  wandb:
    job_type: expert-ppo-10m-all-scenarios-large-capacity
    tags:
    - issue-791
    - all-scenarios
    - ppo
    - reward-curriculum
    - large-capacity
    - wave-7
    - broad-training
