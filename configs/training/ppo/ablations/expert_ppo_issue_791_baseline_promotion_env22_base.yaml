# Shared base for the issue-791 baseline promotion env22 variants.
# The 1m and 3m variants retain launch identity and override only their policy
# id, env width, budget, checkpoint cadence, and W&B metadata.
# Inherits common invariant settings from expert_ppo_issue_791_promotion_shared_base.yaml.
# Launch a variant file, not this base config.
base_config: expert_ppo_issue_791_promotion_shared_base.yaml
scenario_config: ../../../scenarios/classic_interactions_francis2023.yaml
env_overrides:
  predictive_foresight_device: cuda
tracking:
  wandb:
    enabled: true
    project: robot_sf
    group: issue-791-baseline
